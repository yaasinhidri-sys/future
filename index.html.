<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FUTURE | نظام الإدراك الذكي</title>
    <style>
        body { background: #000; color: #00f2fe; font-family: sans-serif; display: flex; align-items: center; justify-content: center; height: 100vh; margin: 0; text-align: center; }
        .box { border: 2px solid #00f2fe; padding: 40px; border-radius: 20px; box-shadow: 0 0 30px #00f2fe; }
        input { background: #111; border: 1px solid #00f2fe; color: #fff; padding: 10px; width: 80%; margin-bottom: 20px; text-align: center; }
        button { background: #00f2fe; color: #000; border: none; padding: 10px 25px; border-radius: 5px; font-weight: bold; cursor: pointer; }
        #system { display: none; }
    </style>
</head>
<body>
    <div id="login" class="box">
        <h1>FUTURE</h1>
        <p>أدخل كود المرور الخاص بك:</p>
        <input type="password" id="pin" placeholder="*****">
        <br>
        <button onclick="check()">دخول</button>
    </div>
    <div id="system" class="box">
        <h1>نشط الآن ✅</h1>
        <p>مرحباً بك يا صاحب Future. النظام متصل بمحركات البحث.</p>
    </div>
    <script>
        function check() {
            if(document.getElementById("pin").value === "24262") {
                document.getElementById("login").style.display = "none";
                document.getElementById("system").style.display = "block";
            } else { alert("الكود خاطئ!"); }
        }
    </script>
</body>
</html>
